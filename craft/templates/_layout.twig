<!DOCTYPE html>
<html>
<head>
  <title>{{ entry is defined ? entry.title }}</title>
  <link rel="stylesheet" href="{{ siteUrl }}jordan.css">
  <meta charset="utf-8"/>
</head>

<body>
  {% block content %}
  {% endblock %}
</body>
</html>